type = custom/script
interval = 1
format =  <label>
exec = mocp-state.sh
format-background = ${colors.background-alt}
format-foreground = ${colors.green}

click-left = mocp --toggle-pause
click-middle = mocp --next
click-right = mocp --previous

