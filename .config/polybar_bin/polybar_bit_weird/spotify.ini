type = "custom/script"
interval = 1
format = <label>
format-background = ${colors.background-alt}
format-foreground = ${colors.green}
exec = showmus

click-left = controls toggle
click-middle = controls prev
click-right = controls next
