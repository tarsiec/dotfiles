type = custom/script
interval = 1
exec = "echo "$(mpd-state.sh)" "$(mpc current)""
format-background = ${colors.background-alt}
format-foreground = ${colors.green}

click-left = mpc toggle
click-middle = mpc prev
click-right = mpc next

